import { defineMessages } from 'react-intl';

export const scope = 'components.Layout.NextFormButton';

export default defineMessages({
  nextForm: {
    id: `${scope}.nextForm`,
    defaultMessage: 'Next Form'
  }
});
